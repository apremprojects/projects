#include <string>
#include <vector>
#include <array>
#include <fstream>
#include <algorithm>
#include <iostream>

using namespace std;

int conv(const int num, const int M) {
	int place = 1;
	int total = 0;
	int remainder = num;
	return -1;
}

int main() {
	int num;
	int M;
	cin >> num >> M;
	cout << conv(num, M);
}